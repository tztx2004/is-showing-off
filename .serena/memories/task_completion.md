# Completion Guidelines

Before finishing a task:
1. Ensure the code is linted using `pnpm lint`.
2. Verify that there are no TypeScript errors.
3. Check the application visually by running `pnpm dev` if UI changes were made.
4. Ensure the UI is responsive and accessible.
5. Do not leave debug logs or unused code.
